import React from 'react';
import style from '../../styles/pageStyles/{{lowerCase name}}.scss';

const {{name}} = () => {
  return (
    <div className="{{name}}">
      <p>{{name}}</p>
    </div>
  );
};

export default {{name}};
